@code {
#pragma warning disable 4014, 1998, 039, CSS041
    static public string BoolStyle(bool b) => b ? "coolButtonX" : "coolButton";
    //static public readonly string TextColor = "#00FFFF";
    //static public readonly string TextColor2= ConsoleColor.Yellow.ToString();
    //static public readonly string GradientBody =
    //    $"linear-gradient(to left, RGB(25,25,64), RGB(0,0,0))";

}
<style>
    * {
        --BC: 180;
        --TextColor: hsl(var(--BC), 50%, 80%);
        --TextColor2: Yellow;
        --BackgroundBase: black;
        color: var(--TextColor);
        font-family: 'Yu Gothic'; /*Rockwell';*/
        font-weight: 300;
    }

    html {
        font-size: 18px;
    }

    :root {
        --GradientBody: linear-gradient(to left, hsl(var(--BC),100%,5%), var(--BackgroundBase));
    }

    body {
        background-image: var(--GradientBody);
    }

    .coolButton, .coolSelect, .coolButtonX {
        background-color: transparent;
        border-radius: 0.2rem;
        border: 0.1rem solid lightgray;
        margin: 0.1rem;
        border: 0.1rem solid var(--TextColor);
    }

    .coolButtonX {
        border: 0.1rem solid yellow;
        color: black;
        background-color: var(--TextColor);
        font-weight: 900;
    }

    .coolButton:hover, .coolSelect:hover, .coolButtonX:hover {
        border: 0.1rem solid red;
    }

    .coolButton, .coolSelect, .coolButtonX {
        width: 1.7rem;
        padding: 0.1rem 0.2rem 0.1rem 0.2rem;
    }


    option {
        color: var(--TextColor);
        background-color: var(--BackgroundBase);
    }

    .coolDiv {
        border: 0.05rem solid gray; /*var(--ControlColor);*/
        margin: 0.2rem;
    }

    .coolSelect {
        padding: 0.2rem 0.1rem;
        width: auto;
    }

    .imgSmileyButton, .imgCheck, .imgSmileSmall {
        height: 1.8rem;
        margin: 0rem 0rem 0.2rem 0rem;
    }

    .imgCheck {
        height: 1.33rem;
    }


    .imgSmileyButton:hover {
        border-radius: 100%;
        cursor: pointer;
    }

    .imgSmileySmall {
        height: 1.2em;
        padding: 0rem 0rem 0.1rem;
    }

    .btn_ghost {
        width: 0.2rem;
        height: 0.5rem;
        padding: 0;
        margin: 0;
        background-color: transparent;
        border: 0px;
    }


    table.g_blueTable {
        margin: 1rem 0rem;
    }

        table.g_blueTable td, table.g_blueTable th {
            padding: 1px 1px;
            overflow: hidden;
            text-overflow: clip;
            white-space: nowrap;
            
        }

        table.g_blueTable td {
            width: 40%;
        }

            table.g_blueTable td.short {
                width: none;
                max-width: 2rem;
            }

        /*        table.g_blueTable tr:nth-child(5n) {
                background-color: rgba(127, 127, 127, 0.2);
            }*/

        table.g_blueTable thead th {
            color: var(--TextColor2);
            font-weight: 700;
            background-image: var(--GradientEven);
            border-bottom: 1px  solid var(--TextColor);
         
        }

    th, coolSelect, coolButtonX, coolSpan {
        user-select: none;
    }

    table.g_blueTable td, able.g_blueTable tr {
        white-space: nowrap;
        padding: 0.3rem 0rem;
        color: var(--TextColor);
    }

</style>

