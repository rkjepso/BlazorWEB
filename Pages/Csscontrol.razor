@code {
    #pragma warning disable 4014, 1998, 039, CSS041
    static public string BoolStyle(bool b) => b ? "coolButtonX" : "coolButton";
    static public string TextColor2 = ConsoleColor.Yellow.ToString();

}
<style>
    * {
        --BC: 260;
        --TextColor: hsl(var(--BC), 80%, 100%);
        --TextColor2: @TextColor2;
        --BackgroundBase: black;
        --BackG1: black;
        --BackG2: hsl(260,100%,10%);
        color: var(--TextColor);
        font-family:Calibri; /*Yu Gothic';*/ /*Rockwell';*/
        font-weight: 400;
    }

    html {
        font-size: 18px;
    }

    :root {
        --GradientBody: linear-gradient(to right, var(--BackgroundBase), var(--BackG2));
    }

    body {
        background-image: var(--GradientBody);
    }

    .coolButton, .coolSelect, .coolButtonX {
        background-color: transparent;
        border-radius: 0.2rem;
        margin: 0.1rem;
        border: 0.1rem solid var(--TextColor);
    }

    .coolButtonX {
        border: 0.10rem solid red;
        color: var(--TextColor2);
        background-color:var(--BackG1);
    }

    .coolButton:hover, .coolSelect:hover, .coolButtonX:hover {
        border: 0.1rem solid var(--TextColor2);
    }

    .coolButton, .coolSelect, .coolButtonX {
        width: 1.7rem;
        padding: 0.1rem 0.2rem 0.1rem 0.2rem;
    }

    option {
        color: var(--TextColor);
        background-color: var(--BackgroundBase);
    }

    label {
        margin:0rem;
        padding:0rem;

    }


    .coolDiv {
        border: 0.05rem solid gray; /*var(--ControlColor);*/
        margin: 0.2rem;
    }

    .coolSelect {
        padding: 0.2rem 0.0rem;
        margin:0rem;
   /*     width: auto;*/
    }

    .imgSmileyButton,  .imgSmileSmall {
        height: 1.8rem;
        margin: 0rem 0rem 0.2rem 0rem;
    }

    .imgSmileyButton:hover {
        border-radius: 100%;
        cursor: pointer;
    }

    .imgSmileySmall {
        height: 1.2em;
        padding: 0rem 0rem 0.1rem;
    }

    .btn_ghost {
        width: 0.2rem;
        height: 0.5rem;
        padding: 0;
        margin: 0;
        background-color: transparent;
        border: 0px;
    }


    table.g_blueTable {
        margin: 1rem 0rem;
    }

        table.g_blueTable td, table.g_blueTable th {
            padding: 1px 1px;
            overflow: hidden;
            text-overflow: clip;
            white-space: nowrap;
        }

        table.g_blueTable td {
            width: 40%;
            user-select: none;
        }

            table.g_blueTable td.short {
                width: none;
                max-width: 2rem;
            }

        /*        table.g_blueTable tr:nth-child(5n) {
                    background-color: rgba(127, 127, 127, 0.2);
                }*/

        table.g_blueTable thead th {
            color: var(--TextColor2);
         
            background-image: var(--GradientEven);
            border-bottom: 1px solid var(--TextColor);
        }

    th, coolSelect, coolButtonX, coolSpan {
        user-select: none;
    }

    table.g_blueTable td, able.g_blueTable tr {
        white-space: nowrap;
        padding: 0.3rem 0rem;
        color: var(--TextColor);
    }

</style>

