@code {
    #pragma warning disable 4014, 1998, 039, CSS041
    static public string BoolStyle(bool b) => b ? "coolButtonX" : "coolButton";

    static public string TextColor
    {
        get;
        set;
    } = ConsoleColor.White.ToString();
    static public string TextColor2
    {
        get;
        set;
    } = ConsoleColor.Yellow.ToString();
}
<style>

    :root {

        --GradientBody: linear-gradient(to right, hsl(var(--BC),var(--CI),var(--BDark)), hsl(var(--BC),var(--CI),var(--BLight) ));
        --GradientEven: linear-gradient(to left, hsl(var(--BC),var(--CI),var(--BDark)), hsl(var(--BC),var(--CI),var(--BLight) ));
        --TextColor: @TextColor;
        --TextColor2: @TextColor2;
    }

    body {
        background-image: var(--GradientBody);
        font-family:'Malgun Gothic';
  
    }
    * {
        color: var(--TextColor);
    }

    .coolButton, .coolSelect, .coolButtonX {
        background-color: transparent;
        border-radius: 0.2em;
        border: 0.1em solid lightgray; /*var(--ControlColor);*/
        margin: 0.1em;

    }
    .coolButtonX {
        border: 0.1em solid red; /*var(--ControlColor);*/
        color:var(--TextColor2);
    }

    .coolButton:hover, .coolSelect:hover {
        border: 0.1em solid red;
    }

    option {
        color: black;
    }

    .coolDiv {
        border: 0.05em solid gray; /*var(--ControlColor);*/
        margin: 0.2em;
    }

    .coolSelect {
        padding: 0.2em 0.1em;
    }

    .imgSmileyButton, .xsequental, .xrandom {
        height: 1.7em;
        margin: 0em 0em 0.2em 0em;
    }

    .imgSmileyButton:hover {
        border: 0.15em solid red;
        border-radius: 100%;
    }


    table.g_blueTable
    {
        margin:1em 0em;
    }

    table.g_blueTable td, table.g_blueTable th {
        padding: 1px 1px;
        width: 33%;
        white-space: nowrap;
    }

    table.g_blueTable tr:nth-child(even) {
        background-image: var(--GradientEven);
        color: white;
    }


    table.g_blueTable thead th {
        color: var(--TextColor);
        font-weight: 900;
        background-image: var(--GradientEven);
    }

 
</style>

